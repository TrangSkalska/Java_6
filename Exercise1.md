`a + b` - arithmetic operator, numeric types (e.g. int, double).
`x >= y` – relational operator, numeric types.
`!finished` – logical operator, boolean.
`progress += 10` – assignment (compound) operator, numeric.
`(condition) ? a : b` – conditional (ternary) operator, boolean + compatible result types.